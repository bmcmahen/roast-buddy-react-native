import React from "react";
import { Base } from "../components";

import { Animated } from "react-native";

import { connect } from "react-redux";

// 2 states
// isRecording: boolean;
// isCollapsed: boolean;

// when you press + to record a new roast,
// either it creates a new one, or it expands the already
// existing roast.

// when collapsed, you can press the bar to expand it.
// when expanded, you get the down arrow type thing at the top that
// you can click or drag to collapse.

// instead of having embedded input selectors, you get a
// modal on the bottom of the screen with a 'done' field.

class RecorderView extends React.Component {
  render() {}
}

export default RecorderView;
